enum GeneralStatus {
  initial,
  loading,
  success,
  error,
}

enum InboxType {
  inbox,
  received,
  sent,
  observed,
  archived,
}

enum MenuEnum {
  dashboard,
  correspondences,
  inbox,
  received,
  sent,
  observed,
  archived,
  employees,
  units,
  reports,
  settings,
}

enum CorrespondenceTypeCode {
  ci,
  ce,
}
