import 'package:flutter/material.dart';

class UiColors {
  // Brand — municipal green refined
  static const primary = Color(0xFF0F766E);
  static const primaryHover = Color(0xFF0D9488);
  static const primarySoft = Color(0xFFCCFBF1);
  static const primaryMuted = Color(0xFF99F6E4);

  // Neutrals — slate scale (US/EU SaaS)
  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceElevated = Color(0xFFFFFFFF);
  static const border = Color(0xFFE2E8F0);
  static const borderLight = Color(0xFFF1F5F9);

  static const sidebar = Color(0xFF0F172A);
  static const sidebarHover = Color(0xFF1E293B);
  static const sidebarActive = Color(0xFF134E4A);

  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textMuted = Color(0xFF94A3B8);
  static const textOnDark = Color(0xFFF8FAFC);
  static const textOnDarkMuted = Color(0xFF94A3B8);

  static const info = Color(0xFF2563EB);
  static const infoSoft = Color(0xFFDBEAFE);
  static const warning = Color(0xFFD97706);
  static const warningSoft = Color(0xFFFEF3C7);
  static const success = Color(0xFF059669);
  static const successSoft = Color(0xFFD1FAE5);
  static const danger = Color(0xFFDC2626);

  // Hero gradients
  static const heroStart = Color(0xFF0F172A);
  static const heroMid = Color(0xFF134E4A);
  static const heroEnd = Color(0xFF0F766E);
}
